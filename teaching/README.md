# Teaching resources

Teaching routes use the maintained education templates in `../report-templates/education`, the public getting-started and study material in `../docs/src`, and executable public examples indexed under `../examples`. This index avoids duplicating canonical lessons, cases, or generated outputs.
